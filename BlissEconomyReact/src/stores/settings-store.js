import { createContext } from "react";

export const moneyHideInitialState = false;
export const MoneyHideContext = createContext();

export const userIdInitialState = "Darth-Vader";
export const UserIdContext = createContext();

